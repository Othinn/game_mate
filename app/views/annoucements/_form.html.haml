= form_for(Annoucement.new) do |f|
  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :description
    = f.text_area :description
  .field
    = f.label :city
    = f.text_field :city
  .field
    = f.label :exp_date
    = f.date_select :exp_date
  .field
    = f.label :group
    =collection_select(:annoucement, :group_id, Group.all, :id, :group_name)
    -#= collection_select(:annoucement, :group_id, UserGroup.where("user_id = ?", current_user[:id]), :id, :group_id)
  .actions
    = f.submit 'Save'
