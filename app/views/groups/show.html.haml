.image-container
  =image_tag @group.group_image.url
%h1
  =@group.group_name
  announcements
.new-announcement-container
  = link_to 'Add announcement', '#',  id: 'new-announcement-link'
  .fas.fa-plus-circle
#new-announcement
  = render 'announcements/form'
%section.announcements#container
  = render @group_announcements